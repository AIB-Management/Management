<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
            http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context
            http://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/mvc
            http://www.springframework.org/schema/mvc/spring-mvc.xsd
            http://www.springframework.org/schema/tx
            http://www.springframework.org/schema/tx/spring-tx.xsd
            http://www.springframework.org/schema/aop
            http://www.springframework.org/schema/aop/spring-aop.xsd "
>

    <!-- 实现邮件服务 -->
    <bean id="mimeMessage" class="javax.mail.internet.MimeMessage"
          factory-bean="javaMailSender" factory-method="createMimeMessage"/>


    <bean id="javaMailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${MAIL_HOST}"/>
        <!--测试用邮箱 -->
        <property name="username" value="${MAIL_USERNAME}"/>
        <property name="password" value="${MAIL_PASSWORD}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">${MAIL_SMTP_AUTH}</prop>
                <prop key="mail.smtp.host">${MAIL_HOST}</prop>
                <prop key="mail.smtp.timeout">${MAIL_SMTP_TIMEOUT}</prop>
                <prop key="mail.smtp.port">${MAIL_SMTP_PORT}</prop>
                <prop key="mail.smtp.socketFactory.port">${MAIL_SMTP_SOCKETFACTORY_PORT}</prop>
                <prop key="mail.smtp.socketFactory.fallback">${MAIL_SMTP_SOCKETFACTORY_FALLBACK}</prop>
                <prop key="mail.smtp.socketFactory.class">${MAIL_SMTP_SOCKETFACTORY_CALSS}</prop>
            </props>
        </property>
    </bean>


</beans>